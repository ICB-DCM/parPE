language: c

compiler: gcc

build:
    ci:
        - sudo apt-get update
        - sudo apt-get install build-essential gfortran libmpich-dev libatlas-dev libhdf5-dev cmake libceres-dev coinor-libipopt-dev libcpputest-dev
        - ./ThirdParty/installAMICI.sh
        - ./buildShippable.sh
        - make test

